import React, { FC } from 'react';
import { AccountSidebar } from '../../../AccountSidebar';

const Account: FC = () => {
  return (
    <main className='main'>
      <AccountSidebar />
    </main>
  );
};
export default Account;
